class User {
  String name, email, password, quantity, rating, credit, status;
  User({
    required this.name,
    required this.email,
    required this.password,
    required this.quantity,
    required this.rating,
    required this.credit,
    required this.status,
  });
}
